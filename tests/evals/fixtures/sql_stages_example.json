{
  "sql_stages": {
    "grounding": {
      "question": "List all employees along with their role name and the territory they work in",
      "expected_response": {
        "tables": ["employees", "roles", "territories"],
        "columns": ["employees.name", "employees.role_id", "employees.territory_id", "roles.name", "territories.name"]
      },
      "judge_criteria": {
        "accuracy_threshold": 8.0,
        "relevance_threshold": 9.0,
        "completeness_threshold": 8.0,
        "hallucination_threshold": 9.0
      }
    },
    "filter": {
      "question": "Show me all active products",
      "grounding_tables": ["products"],
      "grounding_columns": ["is_active", "name", "sku"],
      "expected_response": {
        "where_conditions": ["products.is_active = TRUE"],
        "having_conditions": []
      },
      "judge_criteria": {
        "accuracy_threshold": 9.0,
        "relevance_threshold": 9.0,
        "completeness_threshold": 8.0,
        "hallucination_threshold": 9.0
      }
    },
    "aggregation": {
      "question": "What is the total revenue by customer?",
      "grounding_tables": ["orders", "customers"],
      "grounding_columns": ["customer_id", "grand_total_amount", "company_name"],
      "expected_response": {
        "group_by_columns": ["customers.customer_id", "customers.company_name"],
        "aggregate_functions": ["SUM(orders.grand_total_amount)"],
        "requires_aggregation": true
      },
      "judge_criteria": {
        "accuracy_threshold": 8.0,
        "relevance_threshold": 9.0,
        "completeness_threshold": 8.0,
        "hallucination_threshold": 9.0
      }
    },
    "join": {
      "question": "List all employees with their role names",
      "grounding_tables": ["employees", "roles"],
      "grounding_columns": ["employees.name", "employees.role_id", "roles.name"],
      "expected_response": {
        "joins": [
          {
            "type": "INNER JOIN",
            "left_table": "employees",
            "right_table": "roles",
            "on_condition": "employees.role_id = roles.role_id"
          }
        ],
        "requires_joins": true
      },
      "judge_criteria": {
        "accuracy_threshold": 8.0,
        "relevance_threshold": 9.0,
        "completeness_threshold": 8.0,
        "hallucination_threshold": 9.0
      }
    }
  }
}
